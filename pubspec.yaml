name: painter
description: A simple widget to paint with your fingers. Supports setting a background color, undo and export to png!
version: 2.0.0
repository: https://github.com/EPNW/painter

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"
  
dependencies:
  flutter:
    sdk: flutter
